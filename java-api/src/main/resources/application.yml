spring:
  application:
    name: java-api
  datasource:
    url: jdbc:h2:mem:airlinedb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8081

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

# Demo only: a real deployment reads secrets like this from a secrets
# manager (see Section 8 of the migration plan), not application.yml.
app:
  jwt:
    secret: "java-api-demo-jwt-secret-please-change-in-production-1234567890"
  flights:
    low-seat-threshold: 10
  loyalty:
    silver-threshold: 25000
    gold-threshold: 50000
