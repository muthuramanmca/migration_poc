# java-api

See [`java-api/README.md`](java-api/README.md) — the authoritative, maintained README for the
airline ticket-booking source application (Identity, Flights, Bookings, Notifications, Loyalty).

This root-level copy previously existed as a "preview" of the project before `dummy_api_scaffold.py`
had been run to materialize it. `java-api/` now exists directly and is maintained as real source, not
generated from the (now-stale) scaffold script, so duplicating its README here just risked drift —
kept as a one-line pointer instead.
